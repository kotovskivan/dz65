<!doctype html>
<html lang="uk">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title><%= typeof title !== 'undefined' ? title : 'DZ65' %></title>
  <link rel="stylesheet" href="/public/css/base.css"/>
</head>
<body>
<header class="hdr">
  <a class="brand" href="/">DZ65</a>
  <nav class="nav">
    <a href="/">Home</a>
    <a href="/api/items">API</a>
    <% if (!currentUser) { %><a href="/register">Register</a> <a href="/login">Login</a><% } %>
    <% if (currentUser) { %><a href="/protected">Protected</a><% } %>
  </nav>
  <% if (currentUser) { %>
    <form method="post" action="/logout"><button class="btn">Вийти (<%= currentUser.username %>)</button></form>
  <% } %>
</header>
<main class="container">
  <%- body %>
</main>
<footer class="ftr">
  <p>© <%= new Date().getFullYear() %> Demo</p>
</footer>
</body>
</html>