<% if (typeof layout === 'function') { layout('layout') } %>
<h1>DZ65</h1>
<p>Express + Passport + MongoDB CRUD</p>
<ul>
  <li>Читання (публічне): <code>/api/items</code></li>
  <li>Зміни (авторизація): <code>POST/PATCH/PUT/DELETE</code> на <code>/api/items</code></li>
</ul>